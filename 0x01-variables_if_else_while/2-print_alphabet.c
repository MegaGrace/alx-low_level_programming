#include<stdio.h>

/**
*main - Starting point
*Description - converting alphabets to lowercase
*Return: Always 0 (Success)
*/
int main(void)
{
char ac;
for (ac = 'a'; ac <= 'z'; ac++)
{
putchar(ac);
}
putchar('\n');
return (0);
}
