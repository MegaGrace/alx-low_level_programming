#include<stdio.h>

/**
*main - Starting point
*Description - converting alphabets to lowercase
*Return 0
*/

int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
