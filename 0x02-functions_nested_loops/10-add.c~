#include "main.h"

/**
 *add: adds two integers
 *@a: first integer
 *@b: second integer
 *Result: returns the added result
 */

int add(int a, int b)
{
int result;
result = a + b;
{
return (result);
}
}
