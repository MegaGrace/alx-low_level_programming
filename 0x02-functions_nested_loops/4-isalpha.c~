#include "main.h"

/**
 *_isalpha - checks for alphabetic character
 *@c: character to check alphabets
 *
 *Return: value is 1 if letter, otherwise 0
 */

int _isalpha(int c);
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
return (1);
else
return (0);
}
